name: yndx_todo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gap:
  cupertino_icons:
  logger:
  flutter_bloc:
  bloc:
  bloc_concurrency:
  freezed_annotation: 
  freezed:
  http:


dev_dependencies:
  flutter_lints: ^3.0.0
  build_runner:
  flutter_test: 
    sdk: flutter
  
  

flutter:
  uses-material-design: true
